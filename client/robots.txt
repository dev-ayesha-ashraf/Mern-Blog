# Allow all crawlers access to everything
User-agent: *
Disallow:

# Disallow specific directories or files (example)
# Disallow: /private/
# Disallow: /api/
# Disallow: /login

# Allow specific crawlers access to everything
User-agent: Googlebot
Disallow: